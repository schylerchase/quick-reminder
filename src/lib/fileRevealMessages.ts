export function getFileRevealMissingActiveFileNotice(): string {
  return "Open a note before revealing it in Files.";
}

export function getFileRevealSuccessNotice(): string {
  return "Revealed the active note in Files.";
}

export function getFileRevealFailedNotice(): string {
  return "Quick Reminder could not reveal this note. Open the note manually or enable the Files core plugin.";
}
