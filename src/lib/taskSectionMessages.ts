export function getTaskSectionsMissingNoteNotice(): string {
  return "Open a Markdown note before inserting task sections.";
}

export function getTaskSectionsAlreadyPresentNotice(): string {
  return "This note already has a Tasks section. Use the existing section or scan the dashboard.";
}

export function getTaskSectionsInsertedNotice(): string {
  return "Task sections inserted. Add tasks under a heading, then scan the dashboard.";
}
